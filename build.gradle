allprojects {
  buildscript {
    repositories {
      jcenter()
      mavenCentral()
    }
  }
  dependencies {
    repositories {
      jcenter()
      mavenCentral()
    }
  }

  group = GROUP
  version = VERSION_NAME

  apply plugin: 'maven'
}
